1. Install the dependencies by doing "pip install -r requirements.txt" in the cmd for python packages.
2. Double click on Run file to start the UI.
3. Instance Segmentation will only work if you have Detectron2 installed.